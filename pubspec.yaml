name: agenda_sync
description: AgendaSync Flutter Version
version: 1.0.0+1

environment:
  sdk: '^3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.2.2
  glassmorphism: ^3.0.0
  google_fonts: ^6.1.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  table_calendar: ^3.2.0
  firebase_core: ^4.9.0
  firebase_messaging: ^16.2.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true